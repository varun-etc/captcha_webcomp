import { Component, h, Prop } from "@stencil/core";
export class DialogHeaderComponent {
    constructor() {
        /**
         * Defines the color of the dialog header.
         * `alternative`: Alternative background with primary color for the text.
         * `primary`: Primary color schema.
         */
        this.color = 'primary';
    }
    render() {
        return (h("div", { class: ['se-dialog-header', this.color].join(' ') },
            h("div", { class: "flex middle" },
                h("slot", null)),
            h("slot", { name: "end" })));
    }
    static get is() { return "se-dialog-header"; }
    static get encapsulation() { return "shadow"; }
    static get originalStyleUrls() { return {
        "$": ["dialog-header.scss"]
    }; }
    static get styleUrls() { return {
        "$": ["dialog-header.css"]
    }; }
    static get properties() { return {
        "color": {
            "type": "string",
            "mutable": false,
            "complexType": {
                "original": "'primary' | 'alternative'",
                "resolved": "\"alternative\" | \"primary\"",
                "references": {}
            },
            "required": false,
            "optional": false,
            "docs": {
                "tags": [],
                "text": "Defines the color of the dialog header.\n`alternative`: Alternative background with primary color for the text.\n`primary`: Primary color schema."
            },
            "attribute": "color",
            "reflect": false,
            "defaultValue": "'primary'"
        }
    }; }
}
