import { Component, h, Prop } from '@stencil/core';
export class TooltipHeaderComponent {
    render() {
        return [
            h("div", { class: "tooltip-header" },
                this.icon && h("se-icon", { class: "se-icon", size: "small" }, this.icon),
                h("slot", { name: "icon" }),
                h("slot", null)),
            h("slot", { name: "end" })
        ];
    }
    static get is() { return "se-tooltip-header"; }
    static get encapsulation() { return "shadow"; }
    static get originalStyleUrls() { return {
        "$": ["tooltip-header.scss"]
    }; }
    static get styleUrls() { return {
        "$": ["tooltip-header.css"]
    }; }
    static get properties() { return {
        "icon": {
            "type": "string",
            "mutable": false,
            "complexType": {
                "original": "string",
                "resolved": "string",
                "references": {}
            },
            "required": false,
            "optional": false,
            "docs": {
                "tags": [],
                "text": "Indicates an icon you want to display in your tooltip."
            },
            "attribute": "icon",
            "reflect": false
        }
    }; }
}
