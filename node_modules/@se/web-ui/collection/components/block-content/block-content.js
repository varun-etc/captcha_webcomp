import { Component, Element, h, Prop } from "@stencil/core";
export class BlockContent {
    render() {
        return (h("div", { class: ['se-block-content', this.option ? `${this.option}-content` : ''].join(' ') },
            h("slot", null)));
    }
    static get is() { return "se-block-content"; }
    static get encapsulation() { return "shadow"; }
    static get originalStyleUrls() { return {
        "$": ["block-content.scss"]
    }; }
    static get styleUrls() { return {
        "$": ["block-content.css"]
    }; }
    static get properties() { return {
        "option": {
            "type": "string",
            "mutable": false,
            "complexType": {
                "original": "'card' | 'widget' | 'basic' | 'fill'",
                "resolved": "\"basic\" | \"card\" | \"fill\" | \"widget\"",
                "references": {}
            },
            "required": false,
            "optional": false,
            "docs": {
                "tags": [],
                "text": "The block-content component will add 8px padding on the top and bottom, and 16px padding on the left and right by default.\nWhen the option is set to `fill`, the content will fill the whole space of the block-content with no padding."
            },
            "attribute": "option",
            "reflect": false
        }
    }; }
    static get elementRef() { return "el"; }
}
