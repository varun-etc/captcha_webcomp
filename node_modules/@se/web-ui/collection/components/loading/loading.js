import { Component, h, Host, Prop } from '@stencil/core';
import spinner from "@se/icons/svg/spinner.svg";
export class LoadingComponent {
    constructor() {
        /**
         * Defines the visual display of the loader.
         * `standard` is the default option, and displays a spinning green circle.
         * `dialog` gives the loading icon a look-and-feel that it is within a popup dialog box.
         */
        this.option = 'standard';
    }
    render() {
        return (h(Host, { class: { 'show': this.loading } },
            h("div", { class: "loading-wrapper" },
                h("div", { class: ["se-loading", this.option].join(' ') },
                    h("div", { class: "loading-icon", innerHTML: spinner }))),
            h("div", { class: "loading-background" })));
    }
    static get is() { return "se-loading"; }
    static get encapsulation() { return "shadow"; }
    static get originalStyleUrls() { return {
        "$": ["loading.scss"]
    }; }
    static get styleUrls() { return {
        "$": ["loading.css"]
    }; }
    static get properties() { return {
        "loading": {
            "type": "boolean",
            "mutable": false,
            "complexType": {
                "original": "boolean",
                "resolved": "boolean",
                "references": {}
            },
            "required": false,
            "optional": false,
            "docs": {
                "tags": [],
                "text": "Displays the loading icon on screen when set to `true`."
            },
            "attribute": "loading",
            "reflect": false
        },
        "option": {
            "type": "string",
            "mutable": false,
            "complexType": {
                "original": "'standard' | 'dialog'",
                "resolved": "\"dialog\" | \"standard\"",
                "references": {}
            },
            "required": false,
            "optional": false,
            "docs": {
                "tags": [],
                "text": "Defines the visual display of the loader.\n`standard` is the default option, and displays a spinning green circle.\n`dialog` gives the loading icon a look-and-feel that it is within a popup dialog box."
            },
            "attribute": "option",
            "reflect": false,
            "defaultValue": "'standard'"
        }
    }; }
}
