import { EventEmitter } from '../../stencil-public-runtime';
export declare class RadioGroupComponent {
    el: HTMLElement;
    /**
     * Defines the background color of each button in the group.  The default setting is `standard`, rendering a light gray background.
     */
    color: 'standard' | 'alternative' | 'primary' | 'secondary';
    /**
     * Defines the height of each button in the group.
     * `small` is the default setting, rendering a 32px height and a 14px font size.
     * `nano` sets the height to 24px and the font size to 12px.
     */
    size: 'small' | 'nano';
    /**
     * Optional property that defines if the button is disabled.  Set to `false` by default.
     */
    disabled: boolean;
    /**
     * Defines the selected values of the array.
     */
    value: string;
    /**
     * Passes the selected button value to the parent component when clicking on a button in the group.
     */
    didChange: EventEmitter;
    children: NodeList;
    disabledDidChange(): void;
    valueDidChange(value: any): void;
    buttonClickedHandler(event: CustomEvent): void;
    radioButtonCheckedHandler(event: CustomEvent): void;
    updateItemMode(): void;
    handleChildClicked(event: CustomEvent): void;
    selectChild(children: any, value: any): void;
    componentDidLoad(): void;
    render(): any;
}
