version: '3'

services:
  seicon:
    build: 
      context: ./
      args:
        NODE_ENV: "${NODE_ENV}"
    container_name: se-icon_builder
    env_file:
      - .env
    restart: always
    ports:
      - "443:443"
      - "43554:43554"
      - "80:5000"
