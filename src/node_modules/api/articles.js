import * as api from './api'

export function postFeedback(articleId, lang, feedback){
	return api.post(`/feedback/${articleId}/${lang}`, feedback)
}

export function getRatings(articleId, lang){
	return api.get(`/ratings/${articleId}/${lang}`)
}
